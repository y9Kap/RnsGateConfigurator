{
  "name": "rns-gate_configurator",
  "version": "1.0.23",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/bundle.js --platform=browser",
    "copy:static": "cp index.html dist/index.html && cp styles.css dist/styles.css && mkdir -p dist/assets && cp src/assets/favicon.svg dist/assets/favicon.svg",
    "build:all": "npm run build && npm run copy:static"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  },
  "private": true,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
