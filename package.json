{
  "name": "rns-gate_configurator",
  "version": "1.2.3",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/bundle.js --platform=browser",
    "copy:static": "mkdir -p dist/assets && cp index.html dist/index.html && cp styles.css dist/styles.css && cp src/assets/favicon.svg dist/assets/favicon.svg",
    "build:all": "npm run clean && npm run build && npm run copy:static"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  },
  "private": true,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
